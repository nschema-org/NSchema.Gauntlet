{
  "description": "Appends a value to an enum a column already uses.",
  "destructiveActions": "Error",
  "expectations": {
    "postgres": {
      "outcome": "succeeded"
    },
    "sqlserver": {
      "outcome": "bootstrapFailed",
      "because": "SQL Server has no enum type, so the scenario's bootstrap state cannot be expressed."
    },
    "sqlite": {
      "outcome": "bootstrapFailed",
      "because": "SQLite has no enum type, so the scenario's bootstrap state cannot be expressed."
    }
  }
}
