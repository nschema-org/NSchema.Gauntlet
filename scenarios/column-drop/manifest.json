{
  "description": "Drops a column that nothing else references.",
  "destructiveActions": "Allow",
  "expectations": {
    "postgres": { "outcome": "succeeded" },
    "sqlserver": { "outcome": "succeeded" },
    "sqlite": { "outcome": "succeeded" }
  }
}
