--- Everyone the system knows about.
CREATE TABLE {schema}.people (
  id int NOT NULL,
  --- What to call them.
  name varchar(100) NOT NULL,
  CONSTRAINT pk_people PRIMARY KEY (id)
);

--- Just the names.
CREATE VIEW {schema}.person_names AS SELECT name FROM {schema}.people;
