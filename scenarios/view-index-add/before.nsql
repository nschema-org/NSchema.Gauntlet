CREATE TABLE {schema}.orders (
  id int NOT NULL,
  customer_id int NOT NULL,
  CONSTRAINT pk_orders PRIMARY KEY (id)
);

CREATE VIEW {schema}.order_customers AS SELECT id, customer_id FROM {schema}.orders;
