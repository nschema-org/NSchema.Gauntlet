{
  "description": "Adds one composite type and takes another away, leaving the one a column depends on alone. A corpus only ever creates types from empty, so this is the only place a type is dropped from a database that keeps running.",
  "destructiveActions": "Allow",
  "expectations": {
    "postgres": {
      "outcome": "succeeded"
    },
    "sqlserver": {
      "outcome": "bootstrapFailed",
      "because": "SQL Server has no composite type, so the scenario's bootstrap state cannot be expressed."
    },
    "sqlite": {
      "outcome": "bootstrapFailed",
      "because": "SQLite has no composite type, so the scenario's bootstrap state cannot be expressed."
    }
  }
}
