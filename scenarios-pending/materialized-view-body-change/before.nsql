CREATE TABLE {schema}.orders (
  id int NOT NULL,
  total int NOT NULL,
  CONSTRAINT pk_orders PRIMARY KEY (id)
);

CREATE MATERIALIZED VIEW {schema}.order_totals AS SELECT id FROM {schema}.orders;

CREATE UNIQUE INDEX order_totals_id ON {schema}.order_totals (id);
