=== bootstrap ===
Applying schema migration. Changes will be applied to the database.
Updating state store...
Loading project...
Migration will be scoped to: (all)
Computing migration plan...
Plan
────
+ table main.invoices
    + id int not null
    + state main.status not null
    + reference main.code not null

+ enum main.status (draft, posted)

+ domain main.code varchar(10)

+ type main.address (street varchar(100), zip int)

+ sequence main.ticket_no (start=100)

Plan: 8 to add, 0 to change, 0 to destroy.

SQL
───
-- [1/1]
CREATE TABLE "main"."invoices" (
    "id" INTEGER NOT NULL,
    "state" main.status NOT NULL,
    "reference" main.code NOT NULL,
    CONSTRAINT "pk_invoices" PRIMARY KEY ("id")
)
                                                 Diagnostics                                                 
╭──────────┬─────────────┬────────────────────┬─────────────────────────────────────────────────────────────╮
│ Severity │ Source      │ Code               │ Message                                                     │
├──────────┼─────────────┼────────────────────┼─────────────────────────────────────────────────────────────┤
│ error    │ sql-dialect │ unsupported-action │ Sqlite does not support the 'create enum' action.           │
│ error    │ sql-dialect │ unsupported-action │ Sqlite does not support the 'create sequence' action.       │
│ error    │ sql-dialect │ unsupported-action │ Sqlite does not support the 'create domain' action.         │
│ error    │ sql-dialect │ unsupported-action │ Sqlite does not support the 'create composite type' action. │
╰──────────┴─────────────┴────────────────────┴─────────────────────────────────────────────────────────────╯
exit: 1

